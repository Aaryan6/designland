export const metadata = {
  title: 'Heros - DesignLand',
};

import HeroCardMotion from '@/components/core/hero-card-motion';
import ComponentCodePreview from '@/components/website/component-code-preview';

# Hero Background Effects

Wellfound like hero section of floating cards with cursor position.

<ComponentCodePreview
  component={<HeroCardMotion />}
  filePath='core/hero-card-motion'
/>
